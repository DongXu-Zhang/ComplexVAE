model:
  latent_channels: 4
  # three stages → f4 (2 downs)
  encoder_block_out_channels: [128, 256, 512]
  decoder_block_out_channels: [96, 192, 384]
  layers_per_block: 2
  mid_block_add_attention: true
  output_activation: linear
